INCLUDE(FindProtobuf)
FIND_PACKAGE(Protobuf REQUIRED)
INCLUDE_DIRECTORIES(${PROTOBUF_INCLUDE_DIR})
PROTOBUF_GENERATE_CPP(PROTO_SRC PROTO_HEADER gps.proto object.proto vehicle.proto)
ADD_LIBRARY(its-proto STATIC ${PROTO_HEADER} ${PROTO_SRC})